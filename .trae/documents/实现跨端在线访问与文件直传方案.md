## 1. 检查与环境确认
- 确认当前项目结构为 Next.js 16 App Router。
- 确认已安装基础 UI 库（Radix UI, Lucide React）。

## 2. 文件传输功能实现 (P2P 方案)
### A. 依赖安装
- 安装 `peerjs` 用于点对点数据传输。
- 安装 `qrcode.react` 用于生成跨端连接二维码。

### B. 创建传输页面
- 在 `web/src/app/transfer/page.tsx` 创建新路由。
- 实现发送端逻辑：选择文件 -> 初始化 Peer -> 生成包含 Peer ID 的 URL 和二维码。
- 实现接收端逻辑：解析 URL 中的 Peer ID -> 建立连接 -> 接收文件流并触发浏览器下载。

### C. 导航集成
- 在 [Navbar.tsx](file:///d:/business%20bot/%E9%A1%B9%E7%9B%AE/%E4%B8%AA%E4%BA%BA%E7%BD%91%E7%AB%99/web/src/components/Navbar.tsx) 中添加“极速传”菜单项。

## 3. 在线访问方案配置
### A. Cloudflare Tunnel 指南
- 提供一键开启内网穿透的步骤（如使用 `npx cloudflared`）。
### B. Vercel 部署支持
- 检查并优化 `vercel.json` 配置，确保生产环境正常访问。

## 4. 验证测试
- 启动开发服务器，使用移动设备扫描电脑生成的二维码，测试大文件和小文件的传输稳定性。